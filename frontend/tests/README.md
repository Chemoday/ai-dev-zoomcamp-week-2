# frontend/tests/

Vitest + Vue Test Utils. `booking-flow.spec.js` is a smoke test for the
main guest flow: see an available table, book it, and see it turn
reserved — with `../src/services/api` mocked so it runs without a
backend.

Run with `npm test` from `frontend/`.
